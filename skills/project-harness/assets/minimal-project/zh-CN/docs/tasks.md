# 项目任务

这里保存由 Agent 维护的长期项目任务历史。用户可以查看，但任务编号、历史查询、状态、验证证据和已完成记录都由当前项目协调者负责维护。执行单元使用已分配的任务引用；发现新的长期工作时，先返回不带编号的建议。不要记录提示词或纯机械执行过程。

任务使用稳定的 `TASK-0001` 格式编号。协调者在并行分发前，把下一个未使用编号写入本记录完成预留；编号不得重复使用。

## 进行中

当前没有进行中的任务。

## 接下来

当前没有排队等待的任务。

## 已阻塞

当前没有被阻塞的任务。

## 已完成

当前没有已完成的任务。

## 已取消或替代

当前没有已取消或被替代的任务。
